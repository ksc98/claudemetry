---
export const prerender = false;
// /setup is no longer needed: the proxy auto-links your CF Access email to
// your stable user_hash on the first request it sees. Old bookmarks redirect
// to the dashboard.
return Astro.redirect("/", 302);
---
